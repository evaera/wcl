# wcl

`wcl` cli for WarcraftLogs report browsing and analysis.

## Authentication

`WCL_ACCESS_TOKEN` environment variable, optionally loaded from a `.env` file in the cwd:

```dotenv
WCL_ACCESS_TOKEN=xxxxxx
````